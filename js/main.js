document.addEventListener("DOMContentLoaded",
    function(){
        //Render skills
        renderSkills();
        //modal
        initModal();
        // contact-validation
        initContactValidation();
        //theme
        initThemeToggle();
        //projects
        renderProjects();
        // education
        renderTimeline();
        // typing-animation
        initTypingAnimation();
        // scroll-progess
        initScrollProgress();
        // scroll-spy
        initScrollSpy();
        // back-to-top
        initBackToTop();
        // Greeting
        initGreeting();
    }
)